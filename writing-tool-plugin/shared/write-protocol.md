# Write Protocol

After every file write, confirm with a single line:

  Saved [field description] to [path].

Do not emit a pre-write notice. Do not emit a multi-line receipt.
The Write tool already confirms success.
